from treeline import tree, TreeRenderer

# eg1
tree(directory="../AppOO", 
     show_code_structure=True,
     show_file_count=True,
     max_depth=5,
     )

# eg2
#renderer = TreeRenderer(
#    show_code_structure=True,
#    max_depth=5,
#   show_size=True,
#)
#renderer.render("../AppOO")
